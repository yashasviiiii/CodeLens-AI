{
  "metadata": {
    "prTitle": "FIX: validate status type in list_jobs_tool",
    "prNumber": 334,
    "author": "biru-codeastromer",
    "sourceBranch": "fix/list-jobs-status-type-validation",
    "targetBranch": "main",
    "repo": "sktime/sktime-mcp",
    "commit": "98b3d4bf",
    "timestamp": "2026-04-28T04:06:44Z",
    "directChanges": 31,
    "impactedCount": 9,
    "violationsCount": 0,
    "orphansCount": 8
  },
  "files": [
    "src/sktime_mcp/tools/job_tools.py",
    "tests/test_background_jobs.py"
  ],
  "fileContents": {
    "src/sktime_mcp/tools/job_tools.py": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
    "tests/test_background_jobs.py": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()"
  },
  "nodes": [
    {
      "id": "tests/test_background_jobs.py::test_cleanup_old_jobs",
      "name": "test_cleanup_old_jobs",
      "type": "Function",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": true,
      "line_number": 163,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::test_list_jobs_tool_accepts_case_insensitive_status",
      "name": "test_list_jobs_tool_accepts_case_insensitive_status",
      "type": "Function",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": true,
      "line_number": 147,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::test_list_jobs_tool_rejects_non_string_status",
      "name": "test_list_jobs_tool_rejects_non_string_status",
      "type": "Function",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": true,
      "line_number": 137,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::test_list_jobs",
      "name": "test_list_jobs",
      "type": "Function",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": true,
      "line_number": 81,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::test_cancel_job",
      "name": "test_cancel_job",
      "type": "Function",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": true,
      "line_number": 118,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::test_job_updates",
      "name": "test_job_updates",
      "type": "Function",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": true,
      "line_number": 37,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::test_job_creation",
      "name": "test_job_creation",
      "type": "Function",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": true,
      "line_number": 8,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::result",
      "name": "result",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 142,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::job_manager",
      "name": "job_manager",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 120,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::job_id",
      "name": "job_id",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 122,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::job",
      "name": "job",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 128,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::job3",
      "name": "job3",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 88,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::completed_jobs",
      "name": "completed_jobs",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 104,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::count",
      "name": "count",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 171,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::job2",
      "name": "job2",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 87,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::running_jobs",
      "name": "running_jobs",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 100,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::job1",
      "name": "job1",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 86,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::all_jobs",
      "name": "all_jobs",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 96,
      "val": 4
    },
    {
      "id": "tests/test_background_jobs.py::success",
      "name": "success",
      "type": "Variable",
      "file": "tests/test_background_jobs.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 26,
      "layer": "Business Logic",
      "gitDiff": "@@ -190,6 +190,32 @@ def test_cancel_job():\n     job_manager.delete_job(job_id)\n \n \n+def test_list_jobs_tool_rejects_non_string_status():\n+    \"\"\"Non-string status values should return validation errors, not crash.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    for bad_status in (True, 1, [\"running\"], {\"status\": \"running\"}):\n+        result = list_jobs_tool(status=bad_status)\n+        assert result[\"success\"] is False\n+        assert \"Invalid status type\" in result[\"error\"]\n+\n+\n+def test_list_jobs_tool_accepts_case_insensitive_status():\n+    \"\"\"Valid status strings should still work regardless of case.\"\"\"\n+    from sktime_mcp.tools.job_tools import list_jobs_tool\n+\n+    job_manager = get_job_manager()\n+    job_id = job_manager.create_job(\"fit_predict\", \"handle\", \"ARIMA\")\n+    job_manager.update_job(job_id, status=JobStatus.RUNNING)\n+\n+    result = list_jobs_tool(status=\"RUNNING\")\n+\n+    assert result[\"success\"] is True\n+    assert any(job[\"job_id\"] == job_id for job in result[\"jobs\"])\n+\n+    job_manager.delete_job(job_id)\n+\n+\n def test_cleanup_old_jobs():\n     \"\"\"Test cleaning up old jobs.\"\"\"\n     job_manager = get_job_manager()",
      "isOrphan": false,
      "line_number": 125,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::<module>",
      "name": "<module>",
      "type": "Function",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": true,
      "line_number": 1,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::cleanup_old_jobs_tool",
      "name": "cleanup_old_jobs_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 127,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::cancel_job_tool",
      "name": "cancel_job_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 90,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::list_jobs_tool",
      "name": "list_jobs_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 40,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::check_job_status_tool",
      "name": "check_job_status_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 15,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::count",
      "name": "count",
      "type": "Variable",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 139,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::job_manager",
      "name": "job_manager",
      "type": "Variable",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 137,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::msg",
      "name": "msg",
      "type": "Variable",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 111,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::job",
      "name": "job",
      "type": "Variable",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 105,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::jobs",
      "name": "jobs",
      "type": "Variable",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 81,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::status_filter",
      "name": "status_filter",
      "type": "Variable",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 68,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/tools/job_tools.py::logger",
      "name": "logger",
      "type": "Variable",
      "file": "src/sktime_mcp/tools/job_tools.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -56,6 +56,14 @@ def list_jobs_tool(\n     # Convert status string to enum\n     status_filter = None\n     if status is not None:\n+        if not isinstance(status, str):\n+            return {\n+                \"success\": False,\n+                \"error\": (\n+                    f\"Invalid status type '{type(status).__name__}'. \"\n+                    \"Expected one of: pending, running, completed, failed, cancelled\"\n+                ),\n+            }\n         try:\n             status_filter = JobStatus(status.lower())\n         except ValueError:",
      "isOrphan": false,
      "line_number": 12,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/server.py::call_tool",
      "name": "call_tool",
      "type": "Function",
      "file": "src/sktime_mcp/server.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "examples/background_training_example.py::main",
      "name": "main",
      "type": "Function",
      "file": "examples/background_training_example.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    }
  ],
  "links": [
    {
      "id": "link-0",
      "source": "tests/test_background_jobs.py::test_list_jobs_tool_accepts_case_insensitive_status",
      "target": "src/sktime_mcp/tools/job_tools.py::list_jobs_tool",
      "type": "CALLS"
    },
    {
      "id": "link-1",
      "source": "tests/test_background_jobs.py::test_list_jobs_tool_rejects_non_string_status",
      "target": "src/sktime_mcp/tools/job_tools.py::list_jobs_tool",
      "type": "CALLS"
    },
    {
      "id": "link-2",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/job_tools.py::cleanup_old_jobs_tool",
      "type": "CALLS"
    },
    {
      "id": "link-3",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/job_tools.py::cancel_job_tool",
      "type": "CALLS"
    },
    {
      "id": "link-4",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/job_tools.py::list_jobs_tool",
      "type": "CALLS"
    },
    {
      "id": "link-5",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/job_tools.py::check_job_status_tool",
      "type": "CALLS"
    },
    {
      "id": "link-6",
      "source": "examples/background_training_example.py::main",
      "target": "src/sktime_mcp/tools/job_tools.py::cleanup_old_jobs_tool",
      "type": "CALLS"
    },
    {
      "id": "link-7",
      "source": "examples/background_training_example.py::main",
      "target": "src/sktime_mcp/tools/job_tools.py::list_jobs_tool",
      "type": "CALLS"
    },
    {
      "id": "link-8",
      "source": "examples/background_training_example.py::main",
      "target": "src/sktime_mcp/tools/job_tools.py::check_job_status_tool",
      "type": "CALLS"
    }
  ]
}