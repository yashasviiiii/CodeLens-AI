{
  "metadata": {
    "prTitle": "Development of a centralised config system with logging enabled",
    "prNumber": 440,
    "author": "Shashankss1205",
    "sourceBranch": "fix343",
    "targetBranch": "main",
    "repo": "sktime/sktime-mcp",
    "commit": "5641e164",
    "timestamp": "2026-05-04T09:55:43Z",
    "directChanges": 105,
    "impactedCount": 77,
    "violationsCount": 0,
    "orphansCount": 20
  },
  "files": [
    "README.md",
    "src/sktime_mcp/config.py",
    "src/sktime_mcp/runtime/executor.py",
    "src/sktime_mcp/server.py",
    "tests/test_data_target_column_validation.py"
  ],
  "fileContents": {
    "README.md": "@@ -163,6 +163,18 @@ If you are using a virtual environment, or if `sktime-mcp` is not on your `PATH`\n }\n ```\n \n+## ⚙️ Configuration\n+\n+The server can be configured via environment variables:\n+\n+| Environment Variable | Description | Default |\n+|----------------------|-------------|---------|\n+| `SKTIME_MCP_LOG_LEVEL` | Logging verbosity (e.g. `INFO`, `DEBUG`, `WARNING`) | `\"WARNING\"` |\n+| `SKTIME_MCP_LOG_PATH` | Optional file path to output logs to in addition to stderr | (None) |\n+| `SKTIME_MCP_AUTO_FORMAT` | Automatically format time series data on load (`true`/`false`) | `\"true\"` |\n+| `SKTIME_MCP_JOB_MAX_AGE_HOURS` | Maximum age in hours before background jobs are cleared | `24` |\n+| `SKTIME_MCP_JOB_CLEANUP_INTERVAL` | Interval in seconds for periodic job cleanup checks | `3600` |\n+\n ## 📚 Available Tools\n \n ### Discovery & Search",
    "src/sktime_mcp/config.py": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
    "src/sktime_mcp/runtime/executor.py": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
    "src/sktime_mcp/server.py": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
    "tests/test_data_target_column_validation.py": "@@ -50,4 +50,3 @@ def test_load_data_source_returns_error_for_missing_explicit_target_column():\n     assert result[\"error_type\"] == \"ValueError\"\n     assert \"Target column 'sales' not found\" in result[\"error\"]\n     assert \"value\" in result[\"error\"]\n-"
  },
  "nodes": [
    {
      "id": "src/sktime_mcp/runtime/executor.py::<module>",
      "name": "<module>",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 1,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::fit_predict",
      "name": "fit_predict",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 186,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::_discover_demo_datasets",
      "name": "_discover_demo_datasets",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 26,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::release_data_handle",
      "name": "release_data_handle",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 895,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::list_data_handles",
      "name": "list_data_handles",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 872,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::load_data_source_async",
      "name": "load_data_source_async",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 628,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::fit_predict_async",
      "name": "fit_predict_async",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 235,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::format_data_handle",
      "name": "format_data_handle",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 751,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::load_data_source",
      "name": "load_data_source",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 533,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::fit",
      "name": "fit",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 119,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::list_datasets",
      "name": "list_datasets",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 529,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::instantiate_pipeline",
      "name": "instantiate_pipeline",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 391,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::predict",
      "name": "predict",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 145,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::instantiate",
      "name": "instantiate",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 60,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::__init__",
      "name": "__init__",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": true,
      "line_number": 51,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::load_dataset",
      "name": "load_dataset",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 87,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::get_executor",
      "name": "get_executor",
      "type": "Function",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 921,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::Executor",
      "name": "Executor",
      "type": "Class",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 44,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::new_handle",
      "name": "new_handle",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 846,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::most_common_diff",
      "name": "most_common_diff",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 800,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::X",
      "name": "X",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 766,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::logger",
      "name": "logger",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 20,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::metadata",
      "name": "metadata",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 696,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::freq",
      "name": "freq",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 815,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::instance",
      "name": "instance",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 153,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::data_handle",
      "name": "data_handle",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 702,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::pipeline",
      "name": "pipeline",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 482,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::predictions_copy",
      "name": "predictions_copy",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 168,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::all_transformers_except_last",
      "name": "all_transformers_except_last",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 445,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::final_task",
      "name": "final_task",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 450,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::_executor_instance",
      "name": "_executor_instance",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 924,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::full_range",
      "name": "full_range",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 819,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::n_duplicates",
      "name": "n_duplicates",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 777,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::params",
      "name": "params",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 439,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::DEMO_DATASETS",
      "name": "DEMO_DATASETS",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 41,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::estimator_name",
      "name": "estimator_name",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 266,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::module_path",
      "name": "module_path",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 97,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::y",
      "name": "y",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 834,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::changes_made",
      "name": "changes_made",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 768,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::data_result",
      "name": "data_result",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 221,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::validation",
      "name": "validation",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 413,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::adapter",
      "name": "adapter",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 555,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::format_result",
      "name": "format_result",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 594,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::handle_info",
      "name": "handle_info",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 262,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::fh",
      "name": "fh",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 162,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::data",
      "name": "data",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 101,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::job_id",
      "name": "job_id",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 272,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::transformer_pipeline",
      "name": "transformer_pipeline",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 468,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::n_gaps",
      "name": "n_gaps",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 821,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::pipeline_name",
      "name": "pipeline_name",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 505,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::predict_result",
      "name": "predict_result",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 362,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::loader",
      "name": "loader",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 100,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::data_info",
      "name": "data_info",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 764,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::handles",
      "name": "handles",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 879,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::_final_meta",
      "name": "_final_meta",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 611,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::component_instances",
      "name": "component_instances",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 431,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::source_name",
      "name": "source_name",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 268,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::result",
      "name": "result",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 724,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::source_type",
      "name": "source_type",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 646,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::node",
      "name": "node",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 66,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::loop",
      "name": "loop",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 667,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::handle_id",
      "name": "handle_id",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 506,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::validator",
      "name": "validator",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 412,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::time_diffs",
      "name": "time_diffs",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 798,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::fit_result",
      "name": "fit_result",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 229,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::params_list",
      "name": "params_list",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 432,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::module",
      "name": "module",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 99,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::parts",
      "name": "parts",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 98,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::n_missing",
      "name": "n_missing",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 833,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/runtime/executor.py::predictions",
      "name": "predictions",
      "type": "Variable",
      "file": "src/sktime_mcp/runtime/executor.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,7 +8,6 @@\n import asyncio\n import inspect\n import logging\n-import os\n import uuid\n from typing import Any\n \n@@ -54,9 +53,9 @@ def __init__(self):\n         self._handle_manager = get_handle_manager()\n         self._job_manager = get_job_manager()\n         self._data_handles = {}  # Store data handles\n-        self._auto_format_enabled = (\n-            os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n-        )\n+        from sktime_mcp.config import settings\n+\n+        self._auto_format_enabled = settings.auto_format\n \n     def instantiate(\n         self,",
      "isOrphan": false,
      "line_number": 164,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/config.py::<module>",
      "name": "<module>",
      "type": "Function",
      "file": "src/sktime_mcp/config.py",
      "prZone": "direct",
      "status": "added",
      "complexityDelta": 3,
      "fileChurn": 62,
      "layer": "Business Logic",
      "gitDiff": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
      "isOrphan": true,
      "line_number": 1,
      "val": 5
    },
    {
      "id": "src/sktime_mcp/config.py::job_max_age_hours",
      "name": "job_max_age_hours",
      "type": "Function",
      "file": "src/sktime_mcp/config.py",
      "prZone": "direct",
      "status": "added",
      "complexityDelta": 3,
      "fileChurn": 62,
      "layer": "Business Logic",
      "gitDiff": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
      "isOrphan": true,
      "line_number": 44,
      "val": 5
    },
    {
      "id": "src/sktime_mcp/config.py::log_level",
      "name": "log_level",
      "type": "Function",
      "file": "src/sktime_mcp/config.py",
      "prZone": "direct",
      "status": "added",
      "complexityDelta": 3,
      "fileChurn": 62,
      "layer": "Business Logic",
      "gitDiff": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
      "isOrphan": true,
      "line_number": 15,
      "val": 5
    },
    {
      "id": "src/sktime_mcp/config.py::auto_format",
      "name": "auto_format",
      "type": "Function",
      "file": "src/sktime_mcp/config.py",
      "prZone": "direct",
      "status": "added",
      "complexityDelta": 3,
      "fileChurn": 62,
      "layer": "Business Logic",
      "gitDiff": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
      "isOrphan": true,
      "line_number": 34,
      "val": 5
    },
    {
      "id": "src/sktime_mcp/config.py::log_path",
      "name": "log_path",
      "type": "Function",
      "file": "src/sktime_mcp/config.py",
      "prZone": "direct",
      "status": "added",
      "complexityDelta": 3,
      "fileChurn": 62,
      "layer": "Business Logic",
      "gitDiff": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
      "isOrphan": true,
      "line_number": 24,
      "val": 5
    },
    {
      "id": "src/sktime_mcp/config.py::job_cleanup_interval_secs",
      "name": "job_cleanup_interval_secs",
      "type": "Function",
      "file": "src/sktime_mcp/config.py",
      "prZone": "direct",
      "status": "added",
      "complexityDelta": 3,
      "fileChurn": 62,
      "layer": "Business Logic",
      "gitDiff": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
      "isOrphan": true,
      "line_number": 53,
      "val": 5
    },
    {
      "id": "src/sktime_mcp/config.py::Settings",
      "name": "Settings",
      "type": "Class",
      "file": "src/sktime_mcp/config.py",
      "prZone": "direct",
      "status": "added",
      "complexityDelta": 3,
      "fileChurn": 62,
      "layer": "Business Logic",
      "gitDiff": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
      "isOrphan": false,
      "line_number": 10,
      "val": 5
    },
    {
      "id": "src/sktime_mcp/config.py::settings",
      "name": "settings",
      "type": "Variable",
      "file": "src/sktime_mcp/config.py",
      "prZone": "direct",
      "status": "added",
      "complexityDelta": 3,
      "fileChurn": 62,
      "layer": "Business Logic",
      "gitDiff": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Configuration module for sktime-mcp.\n+\n+Centralizes environment variables and provides sensible defaults.\n+\"\"\"\n+\n+import os\n+\n+\n+class Settings:\n+    \"\"\"Server and runtime configuration settings.\"\"\"\n+\n+    # -- Runtime & Server Settings --\n+    @property\n+    def log_level(self) -> str:\n+        \"\"\"\n+        Logging level.\n+        Env Var: SKTIME_MCP_LOG_LEVEL\n+        Default: \"WARNING\"\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+\n+    @property\n+    def log_path(self) -> str | None:\n+        \"\"\"\n+        Optional file path to output logs to in addition to stderr.\n+        Env Var: SKTIME_MCP_LOG_PATH\n+        Default: None\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_LOG_PATH\")\n+\n+    # -- Data Formatting --\n+    @property\n+    def auto_format(self) -> bool:\n+        \"\"\"\n+        Whether to automatically format time series data upon load.\n+        Env Var: SKTIME_MCP_AUTO_FORMAT\n+        Default: True\n+        \"\"\"\n+        return os.environ.get(\"SKTIME_MCP_AUTO_FORMAT\", \"true\").lower() == \"true\"\n+\n+    # -- Job Management --\n+    @property\n+    def job_max_age_hours(self) -> int:\n+        \"\"\"\n+        Maximum age in hours before a job is cleaned up.\n+        Env Var: SKTIME_MCP_JOB_MAX_AGE_HOURS\n+        Default: 24\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n+\n+    @property\n+    def job_cleanup_interval_secs(self) -> int:\n+        \"\"\"\n+        Interval in seconds for periodic job cleanup.\n+        Env Var: SKTIME_MCP_JOB_CLEANUP_INTERVAL\n+        Default: 3600\n+        \"\"\"\n+        return int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n+\n+\n+settings = Settings()",
      "isOrphan": false,
      "line_number": 62,
      "val": 5
    },
    {
      "id": "tests/test_data_target_column_validation.py::<module>",
      "name": "<module>",
      "type": "Function",
      "file": "tests/test_data_target_column_validation.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -50,4 +50,3 @@ def test_load_data_source_returns_error_for_missing_explicit_target_column():\n     assert result[\"error_type\"] == \"ValueError\"\n     assert \"Target column 'sales' not found\" in result[\"error\"]\n     assert \"value\" in result[\"error\"]\n-",
      "isOrphan": true,
      "line_number": 1,
      "val": 3
    },
    {
      "id": "tests/test_data_target_column_validation.py::test_load_data_source_returns_error_for_missing_explicit_target_column",
      "name": "test_load_data_source_returns_error_for_missing_explicit_target_column",
      "type": "Function",
      "file": "tests/test_data_target_column_validation.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -50,4 +50,3 @@ def test_load_data_source_returns_error_for_missing_explicit_target_column():\n     assert result[\"error_type\"] == \"ValueError\"\n     assert \"Target column 'sales' not found\" in result[\"error\"]\n     assert \"value\" in result[\"error\"]\n-",
      "isOrphan": true,
      "line_number": 33,
      "val": 3
    },
    {
      "id": "tests/test_data_target_column_validation.py::test_to_sktime_format_rejects_missing_explicit_target_column",
      "name": "test_to_sktime_format_rejects_missing_explicit_target_column",
      "type": "Function",
      "file": "tests/test_data_target_column_validation.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -50,4 +50,3 @@ def test_load_data_source_returns_error_for_missing_explicit_target_column():\n     assert result[\"error_type\"] == \"ValueError\"\n     assert \"Target column 'sales' not found\" in result[\"error\"]\n     assert \"value\" in result[\"error\"]\n-",
      "isOrphan": true,
      "line_number": 14,
      "val": 3
    },
    {
      "id": "tests/test_data_target_column_validation.py::result",
      "name": "result",
      "type": "Variable",
      "file": "tests/test_data_target_column_validation.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -50,4 +50,3 @@ def test_load_data_source_returns_error_for_missing_explicit_target_column():\n     assert result[\"error_type\"] == \"ValueError\"\n     assert \"Target column 'sales' not found\" in result[\"error\"]\n     assert \"value\" in result[\"error\"]\n-",
      "isOrphan": false,
      "line_number": 37,
      "val": 3
    },
    {
      "id": "tests/test_data_target_column_validation.py::data",
      "name": "data",
      "type": "Variable",
      "file": "tests/test_data_target_column_validation.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -50,4 +50,3 @@ def test_load_data_source_returns_error_for_missing_explicit_target_column():\n     assert result[\"error_type\"] == \"ValueError\"\n     assert \"Target column 'sales' not found\" in result[\"error\"]\n     assert \"value\" in result[\"error\"]\n-",
      "isOrphan": false,
      "line_number": 27,
      "val": 3
    },
    {
      "id": "tests/test_data_target_column_validation.py::executor",
      "name": "executor",
      "type": "Variable",
      "file": "tests/test_data_target_column_validation.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -50,4 +50,3 @@ def test_load_data_source_returns_error_for_missing_explicit_target_column():\n     assert result[\"error_type\"] == \"ValueError\"\n     assert \"Target column 'sales' not found\" in result[\"error\"]\n     assert \"value\" in result[\"error\"]\n-",
      "isOrphan": false,
      "line_number": 35,
      "val": 3
    },
    {
      "id": "tests/test_data_target_column_validation.py::adapter",
      "name": "adapter",
      "type": "Variable",
      "file": "tests/test_data_target_column_validation.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 1,
      "fileChurn": 10,
      "layer": "Business Logic",
      "gitDiff": "@@ -50,4 +50,3 @@ def test_load_data_source_returns_error_for_missing_explicit_target_column():\n     assert result[\"error_type\"] == \"ValueError\"\n     assert \"Target column 'sales' not found\" in result[\"error\"]\n     assert \"value\" in result[\"error\"]\n-",
      "isOrphan": false,
      "line_number": 16,
      "val": 3
    },
    {
      "id": "src/sktime_mcp/server.py::<module>",
      "name": "<module>",
      "type": "Function",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": true,
      "line_number": 1,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::call_tool",
      "name": "call_tool",
      "type": "Function",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 625,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::list_tools",
      "name": "list_tools",
      "type": "Function",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 142,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::main",
      "name": "main",
      "type": "Function",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 839,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::sanitize_for_json",
      "name": "sanitize_for_json",
      "type": "Function",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 84,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::_periodic_job_cleanup",
      "name": "_periodic_job_cleanup",
      "type": "Function",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 808,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::run_server",
      "name": "run_server",
      "type": "Function",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 823,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::mcp_stdout",
      "name": "mcp_stdout",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 831,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::job_manager",
      "name": "job_manager",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 815,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::result",
      "name": "result",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 787,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::_PANDAS_AVAILABLE",
      "name": "_PANDAS_AVAILABLE",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 27,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::_NUMPY_AVAILABLE",
      "name": "_NUMPY_AVAILABLE",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 22,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::_handlers",
      "name": "_handlers",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 70,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::sanitized_result",
      "name": "sanitized_result",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 794,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::validation",
      "name": "validation",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 700,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::logger",
      "name": "logger",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 79,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::original_stdout",
      "name": "original_stdout",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 827,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::removed",
      "name": "removed",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 816,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::validator",
      "name": "validator",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 699,
      "val": 4
    },
    {
      "id": "src/sktime_mcp/server.py::server",
      "name": "server",
      "type": "Variable",
      "file": "src/sktime_mcp/server.py",
      "prZone": "direct",
      "status": "modified",
      "complexityDelta": 2,
      "fileChurn": 34,
      "layer": "Business Logic",
      "gitDiff": "@@ -8,9 +8,12 @@\n import asyncio\n import json\n import logging\n-import os\n+import sys\n+from io import TextIOWrapper\n from typing import Any\n \n+import anyio\n+\n try:\n     import numpy as np\n \n@@ -30,6 +33,7 @@\n from mcp.types import TextContent, Tool\n \n from sktime_mcp.composition.validator import get_composition_validator\n+from sktime_mcp.config import settings\n from sktime_mcp.tools.codegen import export_code_tool\n from sktime_mcp.tools.data_tools import (\n     load_data_source_async_tool,\n@@ -62,19 +66,15 @@\n )\n from sktime_mcp.tools.save_model import save_model_tool\n \n-# ---------------------------------------------------------------------------\n-# Server configuration via environment variables\n-# ---------------------------------------------------------------------------\n-JOB_MAX_AGE_HOURS = int(os.environ.get(\"SKTIME_MCP_JOB_MAX_AGE_HOURS\", \"24\"))\n-JOB_CLEANUP_INTERVAL_SECS = int(os.environ.get(\"SKTIME_MCP_JOB_CLEANUP_INTERVAL\", \"3600\"))\n-\n # Configure logging to stderr with detailed format\n-_LOG_LEVEL = os.environ.get(\"SKTIME_MCP_LOG_LEVEL\", \"WARNING\").upper()\n+_handlers: list[logging.Handler] = [logging.StreamHandler(sys.stderr)]\n+if settings.log_path:\n+    _handlers.append(logging.FileHandler(settings.log_path))\n \n logging.basicConfig(\n-    level=getattr(logging, _LOG_LEVEL, logging.WARNING),\n+    level=getattr(logging, settings.log_level, logging.WARNING),\n     format=\"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n-    handlers=[logging.StreamHandler()],\n+    handlers=_handlers,\n )\n logger = logging.getLogger(__name__)\n # Create MCP server instance\n@@ -810,10 +810,10 @@ async def _periodic_job_cleanup():\n     from sktime_mcp.runtime.jobs import get_job_manager\n \n     while True:\n-        await asyncio.sleep(JOB_CLEANUP_INTERVAL_SECS)\n+        await asyncio.sleep(settings.job_cleanup_interval_secs)\n         try:\n             job_manager = get_job_manager()\n-            removed = job_manager.cleanup_old_jobs(JOB_MAX_AGE_HOURS)\n+            removed = job_manager.cleanup_old_jobs(settings.job_max_age_hours)\n             if removed:\n                 logger.info(f\"Periodic cleanup: removed {removed} old job(s)\")\n         except Exception:\n@@ -822,9 +822,17 @@ async def _periodic_job_cleanup():\n \n async def run_server():\n     \"\"\"Run the MCP server.\"\"\"\n+    # Stdio safety: redirect stdout to stderr to protect MCP JSON-RPC\n+    # streams from being corrupted by stray prints in third-party libraries.\n+    original_stdout = sys.stdout\n+    sys.stdout = sys.stderr\n+\n+    # Explicitly wrap the original stdout buffer for the MCP server output\n+    mcp_stdout = anyio.wrap_file(TextIOWrapper(original_stdout.buffer, encoding=\"utf-8\"))\n+\n     asyncio.create_task(_periodic_job_cleanup())\n \n-    async with stdio_server() as (read_stream, write_stream):\n+    async with stdio_server(stdout=mcp_stdout) as (read_stream, write_stream):\n         await server.run(read_stream, write_stream, server.create_initialization_options())\n \n ",
      "isOrphan": false,
      "line_number": 81,
      "val": 4
    },
    {
      "id": "examples/sql_example.py::<module>",
      "name": "<module>",
      "type": "Function",
      "file": "examples/sql_example.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "examples/csv_example.py::<module>",
      "name": "<module>",
      "type": "Function",
      "file": "examples/csv_example.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/data_tools.py::load_data_source_async_tool",
      "name": "load_data_source_async_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/data_tools.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/data_tools.py::release_data_handle_tool",
      "name": "release_data_handle_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/data_tools.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/data_tools.py::load_data_source_tool",
      "name": "load_data_source_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/data_tools.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "examples/job_management_demo.py::main",
      "name": "main",
      "type": "Function",
      "file": "examples/job_management_demo.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_async_data_loading.py::test_job_resolves_to_data_handle",
      "name": "test_job_resolves_to_data_handle",
      "type": "Function",
      "file": "tests/test_async_data_loading.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_async_data_loading.py::test_async_load_completes",
      "name": "test_async_load_completes",
      "type": "Function",
      "file": "tests/test_async_data_loading.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_async_data_loading.py::test_data_handle_usable",
      "name": "test_data_handle_usable",
      "type": "Function",
      "file": "tests/test_async_data_loading.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_async_data_loading.py::test_invalid_config",
      "name": "test_invalid_config",
      "type": "Function",
      "file": "tests/test_async_data_loading.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/list_available_data.py::list_available_data_tool",
      "name": "list_available_data_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/list_available_data.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/instantiate.py::instantiate_pipeline_tool",
      "name": "instantiate_pipeline_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/instantiate.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/instantiate.py::instantiate_estimator_tool",
      "name": "instantiate_estimator_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/instantiate.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "examples/pandas_example.py::<module>",
      "name": "<module>",
      "type": "Function",
      "file": "examples/pandas_example.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/fit_predict.py::fit_predict_async_tool",
      "name": "fit_predict_async_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/fit_predict.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/fit_predict.py::fit_predict_tool",
      "name": "fit_predict_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/fit_predict.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/fit_predict.py::list_datasets_tool",
      "name": "list_datasets_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/fit_predict.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/fit_predict.py::predict_tool",
      "name": "predict_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/fit_predict.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_nan",
      "name": "test_nan",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_empty_dict",
      "name": "test_empty_dict",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_empty_list",
      "name": "test_empty_list",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_nested_dict",
      "name": "test_nested_dict",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_full_tool_output",
      "name": "test_full_tool_output",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_dataframe",
      "name": "test_dataframe",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_series",
      "name": "test_series",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_na",
      "name": "test_na",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_nat",
      "name": "test_nat",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_timestamp",
      "name": "test_timestamp",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_2d_array",
      "name": "test_2d_array",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_float_types",
      "name": "test_float_types",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_float_array",
      "name": "test_float_array",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_int_array",
      "name": "test_int_array",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_bool_types",
      "name": "test_bool_types",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_integer_types",
      "name": "test_integer_types",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_list_of_mixed",
      "name": "test_list_of_mixed",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_sanitize.py::test_scalars",
      "name": "test_scalars",
      "type": "Function",
      "file": "tests/test_sanitize.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/evaluate.py::evaluate_estimator_tool",
      "name": "evaluate_estimator_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/evaluate.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_data_sources.py::test_load_and_predict_with_data_handle",
      "name": "test_load_and_predict_with_data_handle",
      "type": "Function",
      "file": "tests/test_data_sources.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_async_custom_data.py::_load_custom_data",
      "name": "_load_custom_data",
      "type": "Function",
      "file": "tests/test_async_custom_data.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_async_custom_data.py::_get_estimator_handle",
      "name": "_get_estimator_handle",
      "type": "Function",
      "file": "tests/test_async_custom_data.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/format_tools.py::auto_format_on_load_tool",
      "name": "auto_format_on_load_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/format_tools.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "src/sktime_mcp/tools/format_tools.py::format_time_series_tool",
      "name": "format_time_series_tool",
      "type": "Function",
      "file": "src/sktime_mcp/tools/format_tools.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    },
    {
      "id": "tests/test_evaluate.py::test_evaluate_estimator_tool",
      "name": "test_evaluate_estimator_tool",
      "type": "Function",
      "file": "tests/test_evaluate.py",
      "prZone": "primary",
      "status": "unchanged",
      "layer": "Business Logic",
      "val": 4
    }
  ],
  "links": [
    {
      "id": "link-0",
      "source": "src/sktime_mcp/runtime/executor.py::<module>",
      "target": "src/sktime_mcp/runtime/executor.py::_discover_demo_datasets",
      "type": "CALLS"
    },
    {
      "id": "link-1",
      "source": "src/sktime_mcp/runtime/executor.py::fit_predict",
      "target": "src/sktime_mcp/runtime/executor.py::fit",
      "type": "CALLS"
    },
    {
      "id": "link-2",
      "source": "src/sktime_mcp/runtime/executor.py::fit_predict",
      "target": "src/sktime_mcp/runtime/executor.py::predict",
      "type": "CALLS"
    },
    {
      "id": "link-3",
      "source": "src/sktime_mcp/runtime/executor.py::fit_predict",
      "target": "src/sktime_mcp/runtime/executor.py::load_dataset",
      "type": "CALLS"
    },
    {
      "id": "link-4",
      "source": "src/sktime_mcp/runtime/executor.py::load_data_source_async",
      "target": "src/sktime_mcp/runtime/executor.py::format_data_handle",
      "type": "CALLS"
    },
    {
      "id": "link-5",
      "source": "src/sktime_mcp/runtime/executor.py::fit_predict_async",
      "target": "src/sktime_mcp/runtime/executor.py::fit",
      "type": "CALLS"
    },
    {
      "id": "link-6",
      "source": "src/sktime_mcp/runtime/executor.py::fit_predict_async",
      "target": "src/sktime_mcp/runtime/executor.py::predict",
      "type": "CALLS"
    },
    {
      "id": "link-7",
      "source": "src/sktime_mcp/runtime/executor.py::fit_predict_async",
      "target": "src/sktime_mcp/runtime/executor.py::load_dataset",
      "type": "CALLS"
    },
    {
      "id": "link-8",
      "source": "src/sktime_mcp/runtime/executor.py::load_data_source",
      "target": "src/sktime_mcp/runtime/executor.py::format_data_handle",
      "type": "CALLS"
    },
    {
      "id": "link-9",
      "source": "src/sktime_mcp/runtime/executor.py::fit",
      "target": "src/sktime_mcp/runtime/executor.py::fit",
      "type": "CALLS"
    },
    {
      "id": "link-10",
      "source": "src/sktime_mcp/runtime/executor.py::instantiate_pipeline",
      "target": "src/sktime_mcp/runtime/executor.py::instantiate",
      "type": "CALLS"
    },
    {
      "id": "link-11",
      "source": "src/sktime_mcp/runtime/executor.py::predict",
      "target": "src/sktime_mcp/runtime/executor.py::predict",
      "type": "CALLS"
    },
    {
      "id": "link-12",
      "source": "src/sktime_mcp/runtime/executor.py::get_executor",
      "target": "src/sktime_mcp/runtime/executor.py::Executor",
      "type": "CALLS"
    },
    {
      "id": "link-13",
      "source": "src/sktime_mcp/config.py::<module>",
      "target": "src/sktime_mcp/config.py::Settings",
      "type": "CALLS"
    },
    {
      "id": "link-14",
      "source": "tests/test_data_target_column_validation.py::test_load_data_source_returns_error_for_missing_explicit_target_column",
      "target": "src/sktime_mcp/runtime/executor.py::Executor",
      "type": "CALLS"
    },
    {
      "id": "link-15",
      "source": "src/sktime_mcp/server.py::<module>",
      "target": "src/sktime_mcp/server.py::call_tool",
      "type": "CALLS"
    },
    {
      "id": "link-16",
      "source": "src/sktime_mcp/server.py::<module>",
      "target": "src/sktime_mcp/server.py::list_tools",
      "type": "CALLS"
    },
    {
      "id": "link-17",
      "source": "src/sktime_mcp/server.py::<module>",
      "target": "src/sktime_mcp/server.py::main",
      "type": "CALLS"
    },
    {
      "id": "link-18",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/data_tools.py::load_data_source_async_tool",
      "type": "CALLS"
    },
    {
      "id": "link-19",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/data_tools.py::release_data_handle_tool",
      "type": "CALLS"
    },
    {
      "id": "link-20",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/data_tools.py::load_data_source_tool",
      "type": "CALLS"
    },
    {
      "id": "link-21",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/instantiate.py::instantiate_pipeline_tool",
      "type": "CALLS"
    },
    {
      "id": "link-22",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/instantiate.py::instantiate_estimator_tool",
      "type": "CALLS"
    },
    {
      "id": "link-23",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/list_available_data.py::list_available_data_tool",
      "type": "CALLS"
    },
    {
      "id": "link-24",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/fit_predict.py::fit_predict_async_tool",
      "type": "CALLS"
    },
    {
      "id": "link-25",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/fit_predict.py::fit_predict_tool",
      "type": "CALLS"
    },
    {
      "id": "link-26",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-27",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/evaluate.py::evaluate_estimator_tool",
      "type": "CALLS"
    },
    {
      "id": "link-28",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/format_tools.py::auto_format_on_load_tool",
      "type": "CALLS"
    },
    {
      "id": "link-29",
      "source": "src/sktime_mcp/server.py::call_tool",
      "target": "src/sktime_mcp/tools/format_tools.py::format_time_series_tool",
      "type": "CALLS"
    },
    {
      "id": "link-30",
      "source": "src/sktime_mcp/server.py::main",
      "target": "src/sktime_mcp/server.py::run_server",
      "type": "CALLS"
    },
    {
      "id": "link-31",
      "source": "src/sktime_mcp/server.py::sanitize_for_json",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-32",
      "source": "src/sktime_mcp/server.py::run_server",
      "target": "src/sktime_mcp/server.py::_periodic_job_cleanup",
      "type": "CALLS"
    },
    {
      "id": "link-33",
      "source": "examples/sql_example.py::<module>",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-34",
      "source": "examples/csv_example.py::<module>",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-35",
      "source": "src/sktime_mcp/tools/data_tools.py::load_data_source_async_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-36",
      "source": "src/sktime_mcp/tools/data_tools.py::release_data_handle_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-37",
      "source": "src/sktime_mcp/tools/data_tools.py::load_data_source_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-38",
      "source": "examples/job_management_demo.py::main",
      "target": "src/sktime_mcp/runtime/executor.py::instantiate",
      "type": "CALLS"
    },
    {
      "id": "link-39",
      "source": "examples/job_management_demo.py::main",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-40",
      "source": "tests/test_async_data_loading.py::test_job_resolves_to_data_handle",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-41",
      "source": "tests/test_async_data_loading.py::test_async_load_completes",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-42",
      "source": "tests/test_async_data_loading.py::test_data_handle_usable",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-43",
      "source": "tests/test_async_data_loading.py::test_invalid_config",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-44",
      "source": "src/sktime_mcp/tools/list_available_data.py::list_available_data_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-45",
      "source": "src/sktime_mcp/tools/instantiate.py::instantiate_pipeline_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-46",
      "source": "src/sktime_mcp/tools/instantiate.py::instantiate_estimator_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-47",
      "source": "examples/pandas_example.py::<module>",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-48",
      "source": "src/sktime_mcp/tools/fit_predict.py::fit_predict_async_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-49",
      "source": "src/sktime_mcp/tools/fit_predict.py::fit_predict_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-50",
      "source": "src/sktime_mcp/tools/fit_predict.py::list_datasets_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-51",
      "source": "src/sktime_mcp/tools/fit_predict.py::predict_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-52",
      "source": "tests/test_sanitize.py::test_nan",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-53",
      "source": "tests/test_sanitize.py::test_empty_dict",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-54",
      "source": "tests/test_sanitize.py::test_empty_list",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-55",
      "source": "tests/test_sanitize.py::test_nested_dict",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-56",
      "source": "tests/test_sanitize.py::test_full_tool_output",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-57",
      "source": "tests/test_sanitize.py::test_dataframe",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-58",
      "source": "tests/test_sanitize.py::test_series",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-59",
      "source": "tests/test_sanitize.py::test_na",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-60",
      "source": "tests/test_sanitize.py::test_nat",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-61",
      "source": "tests/test_sanitize.py::test_timestamp",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-62",
      "source": "tests/test_sanitize.py::test_2d_array",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-63",
      "source": "tests/test_sanitize.py::test_float_types",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-64",
      "source": "tests/test_sanitize.py::test_float_array",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-65",
      "source": "tests/test_sanitize.py::test_int_array",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-66",
      "source": "tests/test_sanitize.py::test_bool_types",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-67",
      "source": "tests/test_sanitize.py::test_integer_types",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-68",
      "source": "tests/test_sanitize.py::test_list_of_mixed",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-69",
      "source": "tests/test_sanitize.py::test_scalars",
      "target": "src/sktime_mcp/server.py::sanitize_for_json",
      "type": "CALLS"
    },
    {
      "id": "link-70",
      "source": "src/sktime_mcp/tools/evaluate.py::evaluate_estimator_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-71",
      "source": "tests/test_data_sources.py::test_load_and_predict_with_data_handle",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-72",
      "source": "tests/test_async_custom_data.py::_load_custom_data",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-73",
      "source": "tests/test_async_custom_data.py::_get_estimator_handle",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-74",
      "source": "src/sktime_mcp/tools/format_tools.py::auto_format_on_load_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-75",
      "source": "src/sktime_mcp/tools/format_tools.py::format_time_series_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    },
    {
      "id": "link-76",
      "source": "tests/test_evaluate.py::test_evaluate_estimator_tool",
      "target": "src/sktime_mcp/runtime/executor.py::get_executor",
      "type": "CALLS"
    }
  ]
}