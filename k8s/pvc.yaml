apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cgc-data-pvc
  labels:
    app: codegraphcontext
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
